<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".Settings"
    android:background="#EEEEEE"
    tools:showIn="@layout/activity_settings">

    <TextView
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/btwhite"
        android:text="Delete a Class"
        android:clickable="true"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:drawablePadding="16dp"
        android:textSize="18sp"
        android:fontFamily="@font/montserrat"
        android:drawableStart="@drawable/ic_delete"
        android:onClick="goToDeleteActivity"/>
    <TextView
        android:layout_marginTop="8dp"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/btwhite"
        android:text="Add a new Student"
        android:clickable="true"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:drawablePadding="16dp"
        android:drawableStart="@drawable/ic_addstudent"
        android:textSize="18sp"
        android:fontFamily="@font/montserrat"
        android:onClick="goToAddStudentActivity"/>

    <TextView
        android:layout_marginTop="8dp"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/btwhite"
        android:text="Remove All Data"
        android:clickable="true"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:drawablePadding="16dp"
        android:drawableStart="@drawable/ic_warningblack"
        android:textSize="18sp"
        android:fontFamily="@font/montserrat"
        android:onClick="deleteAllData"/>

</LinearLayout>